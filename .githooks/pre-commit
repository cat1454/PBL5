#!/bin/sh

repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0

powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass \
  -File "$repo_root/scripts/write-commit-snapshot.ps1" \
  -OutputDirectory "commit-history"
